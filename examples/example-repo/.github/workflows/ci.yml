name: CI

on:
  push:
    branches: [main]
    paths:
      - 'src/**'
      - '!src/**/*.md'
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "run the test suite"
